{
  "name": "cchandoff",
  "version": "0.1.0",
  "description": "Switch Claude Code accounts without burning your usage limits: isolated per-account profiles + automated context handoffs, measured.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "cchandoff": "bin/cchandoff.js",
    "cch": "bin/cchandoff.js"
  },
  "files": [
    "bin",
    "dist",
    "skills",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/",
    "prepack": "npm run build"
  },
  "keywords": [
    "claude",
    "claude-code",
    "handoff",
    "context",
    "usage-limits",
    "prompt-cache",
    "multi-account",
    "cli"
  ],
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
